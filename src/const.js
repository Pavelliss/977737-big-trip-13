const routeTypes = [`taxi`, `bus`, `train`, `ship`, `transport`, `drive`, `flight`, `check-in`, `sightseeing`, `restaurant`];

const TimeMS = {
  day: 86400000,
  hour: 3600000,
  minute: 60000,
};

const SortType = {
  DAY: `DAY`,
  TIME: `TIME`,
  PRICE: `PRICE`,
};


export {
  routeTypes,
  TimeMS,
  SortType,
};
